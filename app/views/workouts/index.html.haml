%h1 All Workouts

%table#workouts
  %thead
    %tr
      %th Title
      %th Time
      %th Creator
      %th Exercises
  %tbody
    - @workouts.each do |workout|
      %tr
        %td= workout.title 
        %td= workout.time
        %td= workout.creator
        %td= link_to "Exercises in #{workout.title}", workout_path(workout)
