%h1 All Workouts

%table#workouts
  %thead
    %tr
      %th Title
      %th Creator
      %th Exercises
  %tbody
    - @workouts.each do |workout|
      %tr
        %td= workout.name 
        %td= workout.profile_id
        %td= link_to "Exercises in #{workout.title}", workout_path(workout)
